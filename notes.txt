🎯 ПРЯМО СЕЙЧАС НУЖНО:
1. ✅ Пользователи (users_api) - УЖЕ ЕСТЬ!
Регистрация/вход/выход - готово

JWT токены - готово

Профиль - готово

2. 🚨 СРОЧНО ДЕЛАТЬ: Посты (echo_api)

3. Добавить API для постов:
GET /api/posts/ - лента постов

POST /api/posts/ - создать пост

POST /api/posts/{id}/echo/ - лайкнуть

POST /api/posts/{id}/disecho/ - дизлайкнуть

GET /api/posts/{id}/comments/ - комментарии к посту

POST /api/posts/{id}/comments/ - добавить комментарий

4. Логика исчезновения:
При создании поста: expires_at = timezone.now() + timedelta(hours=24)

При лайке: expires_at += timedelta(hours=1)

При дизлайке: expires_at -= timedelta(hours=2)

⏳ ПОТОМ СДЕЛАТЬ:
5. Мессенджер (messenger)
Модели Dialog и Message

API для чатов

6. PRO-подписка (subscription)
Модель подписки

Галочка и бейдж для PRO users

🎪 САМОЕ ВКУСНОЕ (но потом):
Плавучие комментарии (когда пост удаляется)

Горизонтальная лента на фронтенде

Задачи Celery для удаления старых постов

🚀 СЕЙЧАС ТВОЙ ПЛАН:
Открыть echo_api/models.py

Написать модели Post и Comment

Сделать python manage.py makemigrations

Сделать python manage.py migrate

Начать делать API для постов

Какой пункт делаем первым? Модели для постов? 🔥

я все таки к слову решил mongщ делать и нужен websocket сказали для месенжера ну чтоб обновляли